import { SwipeDragDirective } from './swipe-drag.directive';

describe('SwipeDragDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeDragDirective();
    expect(directive).toBeTruthy();
  });
});
